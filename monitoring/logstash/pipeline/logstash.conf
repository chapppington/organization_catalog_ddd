input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  # Здесь можно добавить фильтры для обработки логов
  # Например, парсинг определенных форматов, добавление полей и т.д.
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "api-logs"
  }

  # Для отладки - вывод в консоль
  stdout {
    codec => rubydebug
  }
}