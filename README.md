# Organization Catalog DDD

REST API приложение для справочника организаций, зданий и видов деятельности, построенное на принципах Domain-Driven Design.

## 📋 Описание

Приложение предоставляет API для управления каталогом организаций с возможностями:
- Поиска организаций по зданию, виду деятельности, названию
- Геопоиска по координатам (радиус и прямоугольная область)
- Иерархической классификации видов деятельности (до 3 уровней вложенности)
- Получения информации о зданиях и организациях
- Авторизации через JWT токены и API ключи
- Управления пользователями (регистрация, аутентификация)
- Генерации API ключей для доступа к защищенным эндпоинтам

## 🛠 Стек технологий

### Backend
- **Python 3.13** - язык программирования
- **FastAPI** - веб-фреймворк для построения REST API
- **Pydantic** - валидация данных и настройки
- **SQLAlchemy 2.0** - ORM для работы с базой данных
- **Alembic** - миграции базы данных
- **asyncpg** - асинхронный драйвер PostgreSQL

### Инструменты разработки
- **pytest** - тестирование
- **isort** - сортировка импортов
- **pre-commit** - хуки для проверки кода
- **faker** - генерация тестовых данных
- **bcrypt** - хеширование паролей
- **authx** - JWT аутентификация

### Архитектурные паттерны
- **DDD (Domain-Driven Design)** - проектирование на основе предметной области
- **CQRS** - разделение команд и запросов через Mediator
- **Repository Pattern** - абстракция работы с данными
- **Dependency Injection** - через punq контейнер

### Инфраструктура
- **Docker** - контейнеризация
- **PostgreSQL** - основная база данных
- **uvicorn** - ASGI сервер

## 🚀 Запуск проекта

1. Настройте переменные окружения:
```bash
cp .env.example .env
```

2. Запустите приложение:
```bash
make all
```

3. Примените миграции:
```bash
make migrate
```

### Запуск тестов

```bash
# Все тесты
pytest
```

## 🏗 Архитектура DDD

Проект разделен на четыре основных слоя в соответствии с принципами DDD:

### 1. Domain Layer (`app/domain/`)
**Бизнес-логика и правила предметной области**

```
domain/
├── base/                    # Базовые классы
│   ├── entity.py           # Базовая сущность
│   ├── value_object.py     # Базовый объект-значение
│   └── exceptions.py       # Доменные исключения
│
├── organization/           # Контекст организаций
│   ├── entities.py         # Сущности: Organization, Building, Activity
│   ├── value_objects/      # Объекты-значения
│   │   ├── organization_name.py
│   │   ├── organization_phone.py
│   │   ├── building_address.py
│   │   ├── building_coordinates.py
│   │   └── activity_name.py
│   ├── interfaces/         # Интерфейсы репозиториев
│   └── exceptions.py       # Исключения домена
│
└── user/                   # Контекст пользователей
    ├── entities.py         # Сущности: User, APIKey
    ├── value_objects/      # Объекты-значения
    │   ├── username.py
    │   └── password.py
    ├── services/           # Доменные сервисы
    │   ├── user.py        # UserService (создание, аутентификация)
    │   └── api_key.py     # APIKeyService (создание, проверка)
    ├── interfaces/         # Интерфейсы репозиториев
    └── exceptions.py       # Исключения домена
```

**Ключевые сущности:**
- `OrganizationEntity` - организация с названием, телефонами, зданием и видами деятельности
- `BuildingEntity` - здание с адресом и координатами
- `ActivityEntity` - вид деятельности с возможностью древовидной структуры (максимум 3 уровня, настраивается через .env)
- `UserEntity` - пользователь с именем и захешированным паролем
- `APIKeyEntity` - API ключ для аутентификации, связанный с пользователем

**Валидация Value Objects:**
- `ActivityNameValueObject` - имя деятельности (не пустое, максимум 255 символов)
- `BuildingAddressValueObject` - адрес здания (не пустой, максимум 255 символов)
- `OrganizationNameValueObject` - название организации (не пустое)
- `OrganizationPhoneValueObject` - телефон организации (валидация формата: 10-15 цифр)
- `UsernameValueObject` - имя пользователя (не пустое, минимум 3 символа, максимум 255, только буквы, цифры и подчеркивания, начинается с буквы или цифры)
- `PasswordValueObject` - пароль (хранится в захешированном виде с использованием bcrypt)

### 2. Application Layer (`app/application/`)
**Бизнес-процессы и сценарии использования**

```
application/
├── mediator.py            # CQRS медиатор
├── commands/              # Команды (изменение состояния)
│   └── base.py           # Базовые классы команд
├── queries/               # Запросы (чтение данных)
│   └── base.py           # Базовые классы запросов
└── exceptions/            # Исключения слоя приложения
```

**CQRS через Mediator:**
- Команды (Commands) - для операций записи/изменения
- Запросы (Queries) - для операций чтения
- Mediator - маршрутизация запросов к соответствующим обработчикам

### 3. Infrastructure Layer (`app/infrastructure/`)
**Технические детали и внешние зависимости**

```
infrastructure/
└── database/
    ├── main.py            # Конфигурация БД
    ├── models/            # SQLAlchemy модели
    ├── repositories/      # Реализации репозиториев
    │   ├── base.py       # Базовый репозиторий
    │   └── ...           # Конкретные репозитории
    └── converters/        # Конвертеры Entity ↔ Model
```

**Реализации:**
- SQLAlchemy репозитории для работы с PostgreSQL
- InMemory репозитории для тестирования
- Конвертеры между доменными сущностями и моделями БД

### 4. Presentation Layer (`app/presentation/`)
**API endpoints**

```
presentation/
└── api/                   # REST API endpoints
    └── v1/               # Версия API
```

### Settings (`app/settings/`)
**Конфигурация приложения**
- Настройки через Pydantic Settings
- Поддержка переменных окружения

### Tests (`app/tests/`)
**Тестирование**
- Юнит-тесты доменных сущностей
- Тесты value objects
- Интеграционные тесты приложения (commands/queries)
- API тесты (presentation/api/)
  - Тесты создания сущностей (activities, buildings, organizations)
  - Тесты валидации данных
  - Тесты получения данных (по ID, списки, фильтрация)


## 📚 API документация

После запуска доступна по адресам:
- Swagger UI: `http://localhost:8000/api/docs`

### 🔐 Авторизация

API использует два типа авторизации:

1. **JWT токены** (для пользовательских эндпоинтов):
   - Токены хранятся в cookies
   - Access token для коротких операций
   - Refresh token для обновления access token

2. **API ключи** (для всех остальных эндпоинтов):
   - Передаются в заголовке `Authorization: Bearer <api_key>`
   - API ключи создаются через защищенный эндпоинт `/api/v1/user/api-key`
   - Все эндпоинты для activities, buildings, organizations защищены API ключом

### 📡 Основные эндпоинты

#### User (Пользователи) - **Без авторизации**
- `POST /api/v1/user/register` - регистрация нового пользователя
- `POST /api/v1/user/login` - аутентификация и получение токенов
- `POST /api/v1/user/token/refresh` - обновление access token

#### User (API ключи) - **Требует JWT токен**
- `POST /api/v1/user/api-key` - создание API ключа для текущего пользователя

#### Activities (Виды деятельности) - **Требует API ключ**
- `POST /api/v1/activities` - создание вида деятельности
- `GET /api/v1/activities/{activity_id}` - получение по ID
- `GET /api/v1/activities` - список с фильтрацией (name, parent_id)

#### Buildings (Здания) - **Требует API ключ**
- `POST /api/v1/buildings` - создание здания
- `GET /api/v1/buildings/{building_id}` - получение по ID
- `GET /api/v1/buildings` - список с фильтрацией (address, coordinates)

#### Organizations (Организации) - **Требует API ключ**
- `POST /api/v1/organizations` - создание организации
- `GET /api/v1/organizations/{organization_id}` - получение по ID
- `GET /api/v1/organizations?name={name}` - поиск по названию
- `GET /api/v1/organizations/by-address?address={address}` - поиск по адресу
- `GET /api/v1/organizations/by-activity?activity_name={name}` - поиск по виду деятельности
- `GET /api/v1/organizations/by-radius` - геопоиск по радиусу
- `GET /api/v1/organizations/by-rectangle` - геопоиск по прямоугольной области

### 📋 Формат ответов API

Все ответы API возвращаются в едином формате `ApiResponse`:

```json
{
  "data": { ... },           // Данные ответа
  "meta": {},                // Метаинформация
  "errors": []               // Список ошибок (если есть)
}
```

**Примеры ответов:**

Успешный ответ (200/201):
```json
{
  "data": {
    "oid": "uuid",
    "name": "Название",
    ...
  },
  "meta": {},
  "errors": []
}
```

Ошибка валидации (400):
```json
{
  "data": {},
  "meta": {},
  "errors": [
    {"message": "Описание ошибки"}
  ]
}
```

Ошибка валидации запроса (422):
```json
{
  "data": {},
  "meta": {},
  "errors": [
    {
      "message": "body -> name: Field required",
      "type": "missing",
      "field": "body -> name"
    }
  ]
}
```

Ошибка авторизации (401):
```json
{
  "data": {},
  "meta": {},
  "errors": [
    {"message": "API key is required"}
  ]
}
```

### 🔒 Валидация данных

#### Value Objects
- `ActivityNameValueObject` - имя деятельности (не пустое, максимум 255 символов)
- `BuildingAddressValueObject` - адрес здания (не пустой, максимум 255 символов)
- `OrganizationNameValueObject` - название организации (не пустое)
- `OrganizationPhoneValueObject` - телефон организации (валидация формата: 10-15 цифр)
- `UsernameValueObject` - имя пользователя (не пустое, минимум 3 символа, максимум 255, только буквы, цифры и подчеркивания, начинается с буквы или цифры)
- `PasswordValueObject` - пароль (хранится в захешированном виде, валидация на уровне сервиса)

#### HTTP статусы
- `200 OK` - успешный запрос
- `201 Created` - успешное создание
- `400 Bad Request` - ошибка валидации доменных данных или бизнес-логики
- `401 Unauthorized` - отсутствует или неверный API ключ/токен
- `403 Forbidden` - API ключ забанен
- `404 Not Found` - ресурс не найден
- `422 Unprocessable Entity` - ошибка валидации входных данных (формат запроса)